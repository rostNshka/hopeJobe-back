generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Vacancy {
    id   String @id @default(cuid())
    text String
}
